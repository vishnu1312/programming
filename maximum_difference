#include<stdio.h>
int main() {
    int a[7] = {1,3,5,8,12,15,20};
    int b[20];
    int max = 0;
    for (int i=0;i<7;i++) {
      if ((a[i+1] - a[i]) > max) {
            max = a[i+1] - a[i];
      }
    }
    printf("Maximum difference value is %d",max);
}
